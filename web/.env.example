# 前端环境变量样例
# 本地开发：留空即可（使用 vite proxy 转发到 localhost:4000）
# Vercel 部署：在 Vercel 项目设置 -> Environment Variables 中添加
#   VITE_API_BASE = https://YOUR-RENDER-BACKEND.onrender.com
# （即你的 Render 后端地址，不要带结尾斜杠）
VITE_API_BASE=
