# 后端环境变量样例（复制为 .env 后填写；部署到 Render 时在控制台设置）
# 生产环境务必修改 JWT_SECRET 为一个随机长字符串
JWT_SECRET=change-this-to-a-long-random-secret-string

# 服务端口（Render 会自动注入 PORT，无需手动设置）
# PORT=4000

# 数据库目录（Render 持久卷挂载点，例如 /var/data；留空则用服务目录下的 data/）
# DATA_DIR=/var/data
